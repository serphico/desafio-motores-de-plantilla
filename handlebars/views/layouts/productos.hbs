
    <div class="contenedorTable">
      {{#each data}}
    <table class="table">
        <thead>
          <tr>
            
            <th scope="col">{{this.title}}</th>

          </tr>
        </thead>
        <tbody>
          <tr>
            <td><img src="{{this.thumbnail}}" ></td>
          </tr>
          <tr>
            <td>$ {{this.price}}</td>
          </tr>
        </tbody>
    </table>
   {{/each}}
</div>

